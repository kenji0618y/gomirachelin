# GOMIRACHELIN 全体ロードマップ

最終更新：2026-09-06

## 目的

ConoHaからGitHub中心の運用へ安全に移行しながら、地図・ランキング・記事・データ管理など、後で改善する予定の作業も一つの計画で確認できるようにする。

## 基本方針

- 新しいページ・修正・作業記録はGitHubだけに保存する。
- 現在のConoHaサイトは更新せず、移行完了まで旧サイトとして稼働させる。
- GAS地図とGoogle Sheetsは当面継続する。
- ConoHaを解約する前に、ファイルとWordPressデータベースの最終バックアップを保存する。
- GitHub PagesのテストURLで確認が終わるまで、独自ドメインの向き先を変更しない。

## 一目で見る現在地

- **完了**：地図バージョン91、GitHubメニューへの地図追加、AI共通ルール、ロードマップ作成、Pages基本ファイル、サウナ大学画像35枚のGitHub移行。
- **今ここ**：フェーズ1の復旧用バックアップが完成。フェーズ2「GitHubの不足を補完・整理」へ進む。
- **公開前のブロッカー**：現在のGAS地図は未ログイン状態でGoogleログイン画面になる。一般訪問者向けの公開方法が決まるまで、本番メニューへ反映しない。
- **次の3件**：一般公開できる地図方式の決定、GASソースの安全な保存、GitHub Pagesテスト公開の確認。
- **移行と並行して進めること**：施設データの不整合確認、地図の座標補完、ランキング更新の簡略化。
- **移行後に継続すること**：レビュー追加、スマートフォン表示、アクセス解析、SEO、問い合わせと広告リンクの点検。

HTMLの見える化版：`docs/roadmap.html`

## フェーズ0：準備済み

期間：2026-09-05まで

- GitHubに静的サイトの主要ファイルを保存。
- GAS地図をバージョン91へ更新。
- GitHubトップのメニューへ「サウナ地図」を追加。
- `AGENTS.md` を全AI共通ルールの唯一の正文として整備。`CLAUDE.md` と `GEMINI.md` は1行importのみ。
- `docs/STATE.md` と `docs/AI引き継ぎ.md` を整備。

完了条件：GitHubを作業の正本として使える。

## フェーズ1：ConoHaの棚卸しとバックアップ

目安：1週間

- 保存先と安全手順、DNS・メール・契約の記録は完了。
- Web主要データは保存・解凍・ハッシュ確認済み。8,450ファイル、約189.6 MB、内容衝突0件。
- `wp-content/uploads` とサウナ大学元画像45枚を確認済み。
- Web残り10項目（施設5フォルダー、ルート5ファイル）を追加取得する。
- WordPressデータベースをSQLでバックアップする。
- 公開サイトとGitHubの差分一覧は作成済み。内容をフェーズ2の作業へつなげる。
- 画像、PDF、アイコン、`robots.txt`、リダイレクト設定の不足を一覧化する。

完了条件：復旧可能なバックアップと「GitHubに不足しているもの一覧」がある。**2026-09-06完了**。完全スナップショットとの差10項目は記録済み。

## フェーズ2：GitHubの不足を補完・整理

目安：2週間

- 不足しているページと画像だけをGitHubへ追加する。
- `sauna-app.webmanifest` と `sauna-icon-180.png` はPR #3で追加済み。残る不足ファイルを確認する。
- フォルダーを整理する前に、全URLとリンク先の対応表を作る。
- 公開中のGASソースを、秘密情報を含めず `gas/` に保存する。
- 不要ファイルはすぐ削除せず、候補一覧を作ってから小分けに整理する。
- `docs/フェーズ2_補完整理計画.md` に、追加・URL変更・方式決定待ち・公開切替まで保留の4区分を記録済み。

完了条件：公開に必要なページと素材がGitHubにそろい、既知の不足が0件になる。

## フェーズ3：GitHub Pagesのテスト公開

目安：2週間

- 非公開リポジトリのまま有料プランを使うか、安全確認後に公開リポジトリへ変更するか決める。
- GitHub Pagesを一時URLで公開する。
- 必要なら `.nojekyll` と公開フォルダーを設定する。
- パソコンとスマートフォンでトップ、ランキング、レビュー、サウナ大学、地図を確認する。
- 日本語URL、CSS、画像、内部リンク、外部リンク、問い合わせ導線を確認する。
- 404、ブラウザーエラー、HTTP画像の混在を確認する。

完了条件：重大な表示崩れとリンク切れが0件で、主要な利用動線を最後まで操作できる。

## フェーズ4：独自ドメイン切替の準備

目安：1週間

- `sauna-cospa.com` の管理会社と現在のDNS設定を確認する。
- GitHub Pagesで独自ドメインを検証する。
- Webサイト用のA・CNAMEだけを変更する計画を作り、メール用MX・TXTを維持する。
- 切替前のDNS値と、ConoHaへ戻す手順を保存する。
- HTTPSが有効になるまでの確認手順を決める。

完了条件：切替と元に戻す手順が文書化され、メール設定を壊さないことを確認できる。

## フェーズ5：`sauna-cospa.com` を切替・監視

目安：1か月

- 独自ドメインをGitHub Pagesへ向ける。
- HTTPS、主要ページ、GAS地図、アクセス解析を確認する。
- 404と表示崩れを確認し、GitHub側だけを修正する。
- 検索エンジン向けの `sitemap.xml` と `robots.txt` を確認する。
- ConoHaは変更せず、復旧用として残す。

完了条件：30日間、重大な問題がなく、ConoHaへ戻す必要がない。

## フェーズ6：ConoHaを終了

目安：契約期限の1か月前

- ConoHaにしかないファイル、データベース、メールがないことを再確認する。
- 最終バックアップを2か所に保存する。
- ドメイン管理とメールがConoHa解約の影響を受けないことを確認する。
- WordPressとConoHaを解約する。
- `docs/STATE.md` に解約日と最終バックアップ先を記録する。

完了条件：`sauna-cospa.com` がGitHub側で安定稼働し、ConoHaへの依存が0件になる。

## サイト改善ToDo

### 優先A：移行と同時に確認する

- **【決定済み・2026-09-06】** 一般訪問者が開ける地図の公開方法は、**現在のGAS地図（v91）の公開設定を「アクセスできるユーザー：全員」に変更する**方式にした。地図を作り直すより速く確実で、既存の動作を壊さないため。実施にはブラウザ操作が必要。実施後、未ログイン（シークレットウィンドウ）で地図が開けることを確認する。将来ConoHa・GAS依存を完全に無くす場合は、Leaflet＋OpenStreetMapの静的地図をGitHub内に作る案が残る（施設の緯度経度データの書き出しが必要）。
- Claudeブランチ `claude/grok-stopped-tznre4` は丸ごとマージしない。安全確認済みの2ファイルはPR #3で個別に取り込み済み。有用なドキュメントは必要になった時点で個別に確認する。
- ランキング・地図・レビューで施設数、重複、閉店・閉館の扱いを統一する。
- 10評価項目の定義差（⑤・⑦・⑧）を確認し、表記と計算方法をそろえる。
- 「タイパ」が `0.0` の施設を確認し、未入力か意図した値かを整理する。
- 地図の座標未登録施設を確認し、住所と座標を少しずつ補完する。
- GASの最新ソースとGoogle Sheetsのバックアップを、秘密情報を除いてGitHubへ保存する。

### 優先B：GitHub Pagesのテスト公開前に直す

- `sauna-app.webmanifest` と `sauna-icon-180.png` はPR #3で追加し、GitHub側のリンク切れ2件を解消済み。
- `404.html` は不要なHTMLサイトマップ導線を除いた版をGitHub mainへ追加済み。
- `.nojekyll` はGitHub mainへ追加済み。
- `CNAME` はDNSのバックアップと復旧手順が完成するまで追加しない。
- 10項目別ランキングを、1か所のデータから更新できるようにする。
- レビュー一覧を共通メニューから開けるようにする。
- WordPress固有の `/?page_id=37` はトップページで静的な `/why/` へ変更済み（`104896e`）。
- `sauna-daigaku.html` と `sauna-university/index.html` の役割を整理する。
- WordPressの `wp-content/uploads` を参照している画像をGitHub側へ移す。
- 検索エンジン用の `sitemap.xml` と `robots.txt` を確認する。サイトのメニューには追加しない。

### 優先C：移行後も続ける運用

- 新しいレビュー記事の追加手順と `scripts/gen_reviews.py` を使った一覧更新を簡単にする。
- 新規施設を追加するとき、ランキング・地図・レビューを同じデータから更新できる形を目指す。
- パソコンとスマートフォンの表示、リンク切れ、404を定期的に確認する。
- アクセス解析、問い合わせフォーム、Amazonなどの広告リンクを定期的に確認する。
- 作業のたびに `docs/STATE.md` と必要な引き継ぎMarkdownを更新する。

## 完了済み

- GAS地図をバージョン91へ更新し、都道府県境界、掲載施設数、施設ピンを1枚に統合した。
- 地図のFILTER欄を削除した。
- GitHubトップのメニューへ「サウナ地図」を追加した。
- `AGENTS.md` に全AI共通のMarkdown更新ルールを一元化した。ClaudeとGeminiは1行import、GrokとCursorは `AGENTS.md` を直接読む。
- `docs/STATE.md` と `docs/AI引き継ぎ.md` を整備した。
- PR #3で `sauna-app.webmanifest` と `sauna-icon-180.png` だけをmainへ追加し、GitHub側のリンク切れ2件を解消した。既存ファイル、ConoHa本番、DNSは変更していない。
- フェーズ2計画書、`.nojekyll`、`robots.txt`、サイトマップ導線を除いた `404.html` をGitHub mainへ追加した。
- トップの大きなWHYボタンを静的URL `/why/` へ修正し、差分が1行だけであることを確認した（`104896e`）。
- サウナ大学の画像35枚とハッシュ一覧をGitHubへ追加した（`146bfa6`）。
- `sauna-daigaku.html` の画像参照35件をGitHub内パスへ変更した（`0f89c3b`）。

## 現在の次の作業

1. **GASの公開設定を「アクセスできるユーザー：全員」に変更する**（方式は決定済み。ブラウザ操作が必要）。変更後、シークレットウィンドウで地図が開けることを確認する。
2. 地図が未ログインで開けることを確認できたら、全61ページのメニューへ「サウナ地図」を統一追加する（現在は `index.html` のみ）。
3. GitHub Pagesのテスト公開で主要ページ・画像・リンクを確認する。

## 中止条件

- バックアップが作成できない場合は、削除・移動・DNS変更をしない。
- メールの管理先が不明な場合は、DNSを変更しない。
- テスト公開に重大な404や表示崩れがある場合は、独自ドメインを切り替えない。
- 地図が未ログインの一般訪問者に開けない場合は、本番メニューへ地図リンクを追加しない。
- Claudeブランチには撤回済みのHTMLサイトマップ追加が含まれるため、ブランチ全体をそのままマージしない。
