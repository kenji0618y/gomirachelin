# サイトの現在の状態（STATE.md）

このファイルは、サイトの現在の状況・次にやること・過去に決めたことを記録する **作業日誌** です。
**作業を始めるときは必ずこれを読み、作業を終えるときは必ずこれを更新してください。**

**最終更新：2026-09-04（WordPress廃止＋GAS廃止＋GitHub Pages一本化を決定／引継ぎ資料を全面収録）**

---

## ドキュメント一覧（どこに何が書いてあるか）

| ファイル | 内容 | こんなときに読む |
|---|---|---|
| `AGENTS.md` | AIエージェント向け指示書。ルール・禁止事項・Kenjiさんの作業スタイル | **最初に必ず読む** |
| `docs/プロジェクトの理念と背景.md` | サイトが存在する理由。怒りと願い。法的に安全な表現への変換方法 | **2番目に必ず読む** |
| `docs/STATE.md` | 現状・次にやること・決定事項（このファイル） | **3番目に必ず読む** |
| `docs/記事執筆ルール.md` | 文体・SUSURUルール（法的安全）・スコアリング・カラー | 記事やスコアを扱うとき |
| `docs/サイト構成.md` | サイトの構造・データの持ち方の詳細リファレンス | どこを直せば何に反映されるか |
| `docs/作業手順とチェック方法.md` | 動作確認の手順と、実際にハマった落とし穴 | 変更を加える前・確認するとき |
| `docs/過去の失敗と教訓.md` | 実際に起きた失敗の記録と通史 | **同じ失敗を繰り返さないために** |
| `docs/データ元システム.md` | スプレッドシート・GAS・WordPressの詳細（廃止予定の記録） | データの出どころを知りたいとき |
| `docs/GAS廃止計画.md` | GASをやめてGitHubに一本化する計画 | GAS廃止作業をするとき |
| `docs/GitHub-Pages移行手順.md` | WordPress廃止・GitHub Pages移行の手順 | 公開方法の移行作業をするとき |
| `docs/運営とマネタイズ.md` | アフィリエイト・サウナ大学・未完了タスク一覧 | 運営面の作業をするとき |
| `docs/新規レビュー記事の追加手順.md` | サウナを1軒追加する8ステップ | 新しいレビューを追加するとき |
| `README.md` | リポジトリの概要とファイル一覧 | 全体像をざっと知りたいとき |

---

## このサイトは何か

全国のサウナ施設を独自の10項目で評価し、料金（コスパ）をもとに格付け・紹介する
正直ガイドWebサイト「**GOMIRACHELIN（ゴミラシュラン）**」の静的HTMLファイル群です。

- 公開URL：https://sauna-cospa.com
- もとはWordPressサイトをREST APIで読み取り専用に書き出したもの
- **現在は「このリポジトリが唯一の正本」へ移行中**（WordPressは近いうち廃止予定）
- 公開方法も ConoHaへの手動アップロード → **GitHub Pages** へ移行作業中

---

## できていること

### ページ（全63HTMLファイル）

| ページ | ファイル | 備考 |
|---|---|---|
| トップ | `index.html` | キャッチコピー、WHY、10項目の説明、総合トップ3 |
| ランキング | `ランキング/index.html` | 全176施設。総合／コスパの切替つき。**施設データの正本はここ** |
| 10項目別ランキング | `10項目別ランキング/index.html` | 10項目それぞれの順位。全部手書き |
| レビュー一覧 | `reviews/index.html` | 記事のある47軒を新着順。**自動生成**（2026-09-04新設） |
| サイトマップ | `サイトマップ/index.html` | 全ページ一覧を五十音順。**自動生成**（2026-09-04新設） |
| WHY | `why/index.html` | サイトの理念 |
| サウナ大学 | `sauna-university/index.html`, `sauna-daigaku.html` | 2つ併存（理由は下記） |
| 404 | `404.html` | ページが見つからないときの案内（2026-09-04新設） |
| 各種案内 | `お問い合わせ`, `運営者情報`, `プライバシーポリシー`, `免責事項` | |
| 個別レビュー記事 | `{施設名}/index.html` × 50 | 施設47軒＋コラム1本＋重複2本 |

### 機能・仕組み

- **共通スタイル**（`css/site.css`, 322行）：黒背景×金色。ヘッダー／ナビ／フッター／本文
- **施設データ**：`ランキング/index.html` の `FACILITIES`（176件、うち記事あり47件）が唯一の正本
- **自動生成スクリプト**（Python3、標準ライブラリのみ）
  - `scripts/gen_reviews.py` → `reviews/index.html`
  - `scripts/gen_sitemapp.py` → `サイトマップ/index.html`
- **メタデータ**：`data/urls.json`（全ページのid/slug/title/link/date/type）
- **検索エンジン向け**：`sitemap.xml`
- **スマホのホーム画面アイコン**：`sauna-icon-180.png`（180×180、黒×金の「G」）、`sauna-app.webmanifest`
- **GitHub Pages用**：`.nojekyll`、`CNAME`（`sauna-cospa.com`）

### 整合性の状態（2026-09-04時点）

すべて機械的に確認済みです。

- **サイト内リンク切れ：0件**
- **フォルダ / `data/urls.json` / `sitemap.xml` / `FACILITIES` の4者：整合済み**

確認コマンドは `docs/作業手順とチェック方法.md` にあります。

---

## 次にやること

### 0. ★まず決めること：リポジトリを公開にするか★

**GitHub Pagesを無料で使うには、リポジトリを public（公開）にする必要があります。**
現在は private です。→ 詳細と選択肢は `docs/GitHub-Pages移行手順.md` の冒頭。

**さらに重要：GASのデプロイキーがGit履歴に残っています**（調査済み）。
公開する場合は **先にGASのデプロイを停止する**こと。→ 同ファイル参照。

**この判断が済むまで、Pages有効化には進めません。Kenjiさんの回答待ちです。**

### 1. WordPress廃止・GAS廃止・GitHub Pages一本化 ★最優先★

利用者の決定（2026-09-04）：
- **サーバーもGitHub Pagesにする**（時期は近いうち、数日〜数週間以内）
- **GASも廃止する。スプレッドシートはそのまま残す**

手順：`docs/GitHub-Pages移行手順.md` と `docs/GAS廃止計画.md`

- [x] リポジトリ側の準備（`.nojekyll` / `CNAME` / `404.html`）
- [x] 表示面のGAS依存を解消（`script.google.com` への参照0件を確認）
- [ ] **公開/非公開の判断**（上記0）
- [ ] 作業ブランチを `main` に反映する（PR作成の可否をKenjiさんに確認中）
- [ ] GitHub Pages を有効にする ※Kenjiさんの操作が必要
- [ ] DNSをGitHub Pagesに向ける ※Kenjiさんの操作が必要。**ドメイン管理会社が未確認**
- [ ] HTTPSを有効にする
- [ ] 表示確認（特に **日本語URLの記事が開くか**）
- [ ] ConoHa・WordPressを停止
- [ ] GASを停止（停止前にソースをリポジトリへバックアップ）

### 2. ★スプレッドシート → サイトへのデータ反映方法を決める★

GAS廃止後、**スプレッドシートv12の更新をサイトに反映する手段が無くなります。**
`ランキング/index.html` の `FACILITIES` はスプレッドシートのスナップショットです。

推奨案：**CSVでダウンロードしてリポジトリに置き、変換スクリプトで反映する**
（認証不要・どのAIエージェントでも使える・Gitで履歴が残る）

→ 選択肢の比較は `docs/GAS廃止計画.md` の「課題1」。**Kenjiさんの承認待ち。**

### 3. ★10軸の項目名の食い違いを解消する★

**サイトの表示とスプレッドシートの列定義が、10軸中5軸で食い違っています**（2026-09-04に検証）。
⑤⑦⑧は概念そのものが違います。→ `docs/記事執筆ルール.md` の表を参照。

**影響が大きいため、推測で直さずKenjiさんに確認が必要です。**

### 4. その他

- マップ機能（GAS `?page=map`）を静的サイトに移すか、諦めるか（`docs/GAS廃止計画.md` 課題2）
- 記事数50本の達成（実質48本。あと2〜3本）
- `reviews/` を上部メニュー・フッターにも直接足すか

---

## 気になっている点（未解決の課題）

| 課題 | 内容 |
|---|---|
| サウナ大学が2つある | `sauna-daigaku.html`（独自構造の巨大な単独ファイル）と `sauna-university/index.html`（WordPress由来）が併存。前者はAmazonアフィリエイトタグ `gomirachelin-22` を保持するため別管理 |
| 施設データがHTML内にある | `FACILITIES` が `ランキング/index.html` のJavaScript内に直書き。JSONファイルに分離する案もあるが、静的サイトのままだと読み込みが増えるため保留 |
| 10項目別が全部手書き | 1軒追加＝10か所修正。自動化候補（上記「次にやること3」） |
| タイパが `0.0` の施設がある | 例：神戸 クアハウス、SAUNA BUG。意図的か未入力か **利用者に要確認** |
| 自動生成ページはWordPress由来でない | `reviews/` と `サイトマップ/` はこのリポジトリで作ったページ。WordPressから再書き出ししても作られないので、スクリプト実行が必要 |
| HTMLのタグ閉じ忘れが44ファイルにある | WordPress出力に元からあるもの。**直さない**（実害なし、直すとリスクの方が大きい） |
| アイコンは自作 | `sauna-icon-180.png` は元画像が不明だったため新規作成。本番WordPressに元画像があれば差し替え可能 |
| **10軸の名前が2系統ある** | サイト表示とスプレッドシート列定義が5軸で食い違う。⑤⑦⑧は概念が違う。**要確認**（→ `docs/記事執筆ルール.md`） |
| **GASキーがGit履歴にある** | 公開リポジトリにすると露出する。**GASを先に停止すれば無害化できる**（→ `docs/GitHub-Pages移行手順.md`） |
| **スプレッドシート→サイトの反映手段が無い** | GAS廃止後の最大の課題。方式の決定が必要（→ `docs/GAS廃止計画.md` 課題1） |
| **マップ機能が静的サイトに無い** | GASの `?page=map` にのみ存在。GAS廃止で失われる。ただし現在メニューから未リンク（→ 同 課題2） |
| Amazonの支払い・税務情報が未入力 | 未入力だと報酬が受け取れない（→ `docs/運営とマネタイズ.md`） |
| TOTONOI・神戸サウナのJ/K値 | 施設名の不一致で書き込み失敗していた可能性。スプレッドシート要確認 |

---

## 決定事項と理由

### 運用・体制

- **`AGENTS.md` と `docs/STATE.md` を作成した**
  - 理由：作業ルールを統一し、初心者の利用者にもAIエージェントにも現状が伝わるようにするため。
- **`AGENTS.md` を大幅に拡充し、`docs/` にリファレンスを追加した（2026-09-04）**
  - 理由：内容が薄く、別のAIエージェント（Codex）が作業を引き継げなかったため。
    サイト構成・チェック方法・移行手順を明文化した。

### ページ・機能

- **トップの「レビュー一覧を見る」の飛び先を `/category/review/` から `/reviews/` に変更した**
  - 理由：`/category/review/` はWordPressの一覧ページで、静的サイトには書き出されておらず404だったため。
- **レビュー一覧を `/reviews/`（英字フォルダ）で新設した**
  - 理由：WordPress由来でない自作ページだと区別しやすく、アップロード時の日本語フォルダ名の
    文字化けリスクも避けられるため。
- **レビュー一覧は「新着順」にした**
  - 理由：点数順は既に `/ランキング/` があるため、役割を分けて「新しく書いたものから読む入口」にした。
- **自動生成スクリプト方式にした（`gen_reviews.py` / `gen_sitemapp.py`）**
  - 理由：施設データを2か所に書くと必ずズレるため、`FACILITIES` を唯一の正とするため。
- **重複記事2本（`オアシスサウナ-アスティル-2`、`上野ステーションホステル-オリエンタル2-2`）は
  レビュー一覧に載せていない**
  - 理由：同じ施設名が2回並ぶのを避けるため。ページ自体は残してある。
  - なお **サイトマップには載せている**（網羅が目的のため）。
- **サイトマップページを新設し、全ページのメニューとフッターにリンクを追加した**
  - 理由：利用者からの依頼。`sitemap.xml`（検索エンジン向け）とは別に、人が使える一覧が必要だったため。
- **`sauna-icon-180.png` と `sauna-app.webmanifest` を新規作成した**
  - 理由：`sauna-daigaku.html` がこの2ファイルを読み込んでいたが存在せず404だったため。
    元画像が不明だったので、ブランドカラー（黒×金）とロゴの「G」で新規作成した。
- **`404.html` を新規作成した**
  - 理由：GitHub Pagesは独自ドメイン利用時、ルート直下の `404.html` を自動的に使うため。
    素っ気ないエラー画面ではなくサイトデザインに合わせた案内にした。
- **`reviews` を `data/urls.json` に追加した（2026-09-04）**
  - 理由：整合性チェックで、フォルダはあるのに `urls.json` に未登録だったことが判明したため。

### インフラ

- **WordPressを廃止し、サーバーもGitHub Pagesに一本化する**
  - 理由：利用者の希望（2026-09-04確認）。ConoHaへの手動アップロード運用をやめ、
    GitHubにpushするだけで公開される形にする。サーバー代も不要になる。独自ドメインはそのまま使う。
- **`.nojekyll` を追加した**
  - 理由：GitHub Pagesが標準で動かすJekyllは、アンダースコア始まりのフォルダを無視するなど
    勝手な処理をする。素のHTMLサイトには不要かつ有害になりうるため無効化した。
- **`CNAME` を追加した**
  - 理由：GitHub Pagesで独自ドメイン `sauna-cospa.com` を使うために必要なため。
- **GASも廃止し、GitHubに一本化する。ただしスプレッドシートは残す（2026-09-04決定）**
  - 理由：利用者の指示「gasもこれからgithubだけにしたい。スプレッドシートはそのまま」。
    表示面のGAS依存はすでに解消済みで、記事生成・校閲はAIエージェントが直接できるため、
    GASを残す必要性が薄い。スプレッドシートはKenjiさんの採点入力の場として残す。
- **機密情報をドキュメントに書かない方針にした（2026-09-04）**
  - 理由：GitHub Pages無料利用にはリポジトリの公開が必要になる可能性があるため。
    GASのデプロイURL・スプレッドシートIDは、URLを知るだけで悪用されうるので記載しない。

### 引継ぎ資料の収録

- **2026-09-02にClaudeが作成した「Grok移管用 完全引継ぎ資料」を、5つの文書に分割して収録した（2026-09-04）**
  - 理由：サイトの理念・記事執筆ルール・上流システム（スプレッドシート/GAS/WordPress）・
    過去の失敗・運営情報が、会話の中にしか存在せずリポジトリに無かったため。
    別のAIエージェント（Codex）が引き継げなかった根本原因がこれだった。
  - 収録先：`プロジェクトの理念と背景.md` / `記事執筆ルール.md` / `データ元システム.md` /
    `過去の失敗と教訓.md` / `運営とマネタイズ.md`
  - **原資料は「記憶からの再構成」であり、実物と異なる可能性がある**旨を各文書に明記した。

### 作業ルール

- **`sauna-daigaku.html` は一括処理の対象外とする**
  - 理由：他ページと構造がまったく違う（`css/site.css` を読まない、メニューがJavaScriptのbutton形式、
    約4,900行の単独アプリ的ファイル）。一括置換すると壊れるため。
- **WordPress由来のHTMLタグ閉じ忘れは直さない**
  - 理由：44ファイルに元から存在するが、ブラウザは問題なく表示する。直すと差分が巨大になり、
    本文を壊すリスクの方が大きいため。

---

## 作業ログ（2026-09-04）

この日に行った作業を、コミット単位で記録します。

| コミット | 内容 |
|---|---|
| `dcbfb39` | トップのリンク切れ修正、`reviews/` 新設、`gen_reviews.py` 追加、追加手順書を作成 |
| `5fe6355` | `sauna-icon-180.png` / `sauna-app.webmanifest` を新規作成（リンク切れ0件を達成） |
| `260df13` | `サイトマップ/` 新設、全ページのメニュー・フッターにリンク追加、`gen_sitemapp.py` 追加 |
| `9762dbc` | GitHub Pages移行の下準備（`.nojekyll` / `CNAME` / `404.html`） |
| `f500f20` | `README.md` と追加手順書の記述漏れを修正（古い「grok bot 1」の記述も削除） |
| （次） | ドキュメント一式の大幅拡充（`AGENTS.md`、`サイト構成.md`、`作業手順とチェック方法.md`、`GitHub-Pages移行手順.md`） |

**作業ブランチ**：`claude/grok-stopped-tznre4`（まだ `main` に未反映）
