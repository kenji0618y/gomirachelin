# 利用制限前チェックポイント

最終更新：2026-09-05（Claude、セッション上限接近のため保存）

状態：Kenjiさんからセッション上限が近いとの申告あり。新しい作業は開始せず、記録を保存中。

## 現在の作業

- 全体ロードマップのフェーズ1「ConoHaの棚卸しとバックアップ」を進行中。
- ConoHaの対象ドメインは `sauna-cospa.com`。
- Codexが利用制限に達したため、ConoHaへのログイン操作は引き続きKenjiさんが行い、Claudeが手順を文章で案内する形で進行中（Claudeはブラウザ操作ができないため）。
- バックアップ手順は `docs/ConoHaバックアップ手順.md` のフェーズ2「phpMyAdminからDBをSQL形式でエクスポート」まで進んだが、下記の未解決問題で止まっている。

## 未完了・ブロック中の作業（最優先で引き継ぐこと）

- **phpMyAdminに接続できない**：ConoHaコントロールパネルの「データベース」画面→「管理ツール」欄の`phpMyAdmin`リンクをクリックしても、直接URL入力でも、`phpmyadmin2003.conoha.ne.jp`が`ERR_CONNECTION_TIMED_OUT`でタイムアウトする。
- 原因はConoHa側ではなく、Kenjiさんのネット回線・PC側（セキュリティソフト、ルーター等が該当ドメインをブロックしている可能性）と推測。ConoHa側の障害は未確認。
- 提案済みだが**未実施**の切り分け方法：①スマートフォンのモバイル回線で同じリンクを試す、②PCのセキュリティソフトを一時確認、③別のブラウザで試す。
- 代替案：手動SQLエクスポートにこだわらず、ConoHaの「自動バックアップ」機能でDBを含むサーバー全体のバックアップが取得できているかを先に確認する（これでも「もしもの時に戻せる」という目的は満たせる）。
- DBユーザー名`yux6k_x6j43h66`のパスワードはKenjiさんが確認済み。**パスワードやSQLダンプの中身はこのMarkdownにもチャットにも記録していない**（安全のため）。

## 最後に完了した操作

- `index.html`、`sauna-daigaku.html`、`data/urls.json`、`robots.txt`の4ファイル修正をPR #5として作成（未マージ、変更なし）。
- PR #4・PR #5とも本セッション中に2回状態確認（13:15Z、14:18Z）。`mergeable_state: clean`、レビュー・コメント0件で変化なし。60分後（15:21Z）に次の自動チェックインを設定済み。
- `docs/STATE.md`にConoHaバックアップ中断の経緯を追記済み。

## 外部サービスの状態

- GitHub：PR #4（調査文書のみ）、PR #5（小さな安全修正）とも未マージのまま open。「マージせず止めて」の指示が継続中。
- ConoHa：ファイル、WordPress、DB、DNS、メール設定は未変更。SQLエクスポートは未完了（上記の接続問題のため）。パスワードのリセットは行っていない。
- GAS：地図v91のまま変更なし。
- Google Sheets：変更なし。

## 次の1手

1. Kenjiさんにモバイル回線・別ブラウザでのphpMyAdminアクセスを試してもらう。
2. ダメなら「自動バックアップ」機能でDBが含まれているか確認する方向へ切り替える。
3. DBエクスポート（またはその代替）が完了したら、Webデータのファイル取得とDNS・メール記録に進む。

## 中止・復旧条件

- バックアップが完成するまで、ConoHaの削除・移動・上書き、DNS変更、解約を行わない。
- DBユーザーのパスワードリセット（鉛筆マーク）は絶対に使わない。本番WordPressのDB接続が壊れるリスクがあるため。
- wp-config.phpは閲覧のみで、保存・上書きしない。
- 地図が未ログインで開けるようになるまで、本番メニューへ地図リンクを追加しない。
- 問題が起きた場合はConoHa本番をそのまま維持し、GitHub側の変更だけを見直す。
