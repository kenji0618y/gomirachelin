# フェーズ2：GitHub補完・整理計画

最終更新：2026-09-06

## 目的

GitHub Pagesのテスト公開に必要なものを先にそろえ、公開先の切替や削除を伴う作業は安全確認後に行う。

## 先に追加・修正するもの

- `.nojekyll`：日本語名のフォルダーを含む静的ファイルをそのまま配信する。
- `404.html`：存在しないURLからトップやレビュー一覧へ戻れるようにする。
- `robots.txt`：検索エンジンへ `sitemap.xml` の場所を伝える。
- トップの `/?page_id=37` を静的ページ `/why/` へ変更する。
- サウナ大学のConoHa画像参照をGitHub内画像へ切り替える。
- 公開中のGAS v91ソースを、秘密情報とGoogle Sheets IDの実値を除いて`gomirachelin-work/gas-backup-sanitized/` に準備済み（2026-09-06）。公開GitHubへの送信は明示承認待ち。

## 方式を決めてから修正するもの

- サウナ地図：一般公開専用GASかGitHub上の静的地図かを決めてからメニューを統一する。
- レビュー一覧：GitHubの `/reviews/` を正本にし、WordPressの `/category/review/` を切り替える。
- 共通メニューとフォルダー名：既存URLを壊さない対応表を作ってから小分けに整理する。

## 公開切替まで保留するもの

- `CNAME`、DNS変更、ConoHa上の変更・削除、ConoHa解約、GitHubフォルダーの削除。

## 確認したこと

- Web 8,450ファイルとWordPress DB 18テーブルのSQLを保存・検証済み。
- GitHubに `.nojekyll`、`404.html`、`robots.txt`、`CNAME`、`gas/` はない。
- サウナ大学の元画像45枚はバックアップ済み。
- GAS地図はログアウト状態でGoogleログイン画面へ移動する。

## 実施順

1. Pages基本ファイルとWHYリンク修正を準備する。
2. サウナ大学画像をGitHubへ移して表示確認する。
3. GASソースとGoogle Sheetsを秘密情報なしで保存する。
4. 一般公開できる地図方式を決め、未ログインで確認する。
5. メニューとレビューURLを全ページで統一する。
6. GitHub Pagesの一時URLでPC・スマホ・リンク・画像・404を確認する。

## 完了条件

GitHub Pagesに必要なファイルと画像がそろい、一般訪問者がログインせず地図を開け、既知のリンク切れと重大な表示崩れが0件になっている。変更と確認結果は `docs/STATE.md` と `docs/AI引き継ぎ.md` に記録する。
