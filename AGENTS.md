# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェント（Claude Code / Codex / その他）への指示書です。
**作業を始める前に、このファイルを最後まで読んでください。**

---

## 0. 最初にやること（順番厳守）

1. この `AGENTS.md` を読む（← 今ここ）
2. **`docs/プロジェクトの理念と背景.md` を読む** … このサイトがなぜ存在するのか。
   **技術資料より先に読んでください。ここを外すと的外れな提案をします**
3. `docs/STATE.md` を読む … 今のサイトの状態・次にやること・過去の決定事項
4. 作業内容に応じて、次のどれかを読む

| やりたいこと | 読むファイル |
|---|---|
| サイトの構造・データの持ち方を知る | `docs/サイト構成.md` |
| 動作確認・ハマりどころを知る | `docs/作業手順とチェック方法.md` |
| 記事を書く・スコアを扱う | **`docs/記事執筆ルール.md`（法的安全ルール。必読）** |
| 新しいサウナのレビューを追加する | `docs/新規レビュー記事の追加手順.md` |
| データの出どころ（スプレッドシート/GAS/WordPress）を知る | `docs/データ元システム.md` |
| GitHub Pagesへの移行作業をする | `docs/GitHub-Pages移行手順.md` |
| GASとGitHubの役割分担を知る | `docs/GASとGitHubの役割分担.md` |
| 同じ失敗を繰り返さない | **`docs/過去の失敗と教訓.md`（必読）** |
| アフィリエイト・未完了タスクを知る | `docs/運営とマネタイズ.md` |

---

## 1. このプロジェクトは何か（30秒で理解する）

**GOMIRACHELIN（ゴミラシュラン）** は、全国のサウナ施設を独自の10項目で採点し、
料金と照らして「コスパ」で格付けする個人運営のレビューサイトです。

| 項目 | 内容 |
|---|---|
| 公開URL | https://sauna-cospa.com |
| このリポジトリ | https://github.com/kenji0618y/gomirachelin （非公開） |
| 中身 | **素の静的HTML**。ビルド不要、npm不要、フレームワーク不要 |
| 由来 | もとはWordPressサイト。REST APIで読み取り専用に書き出したもの |
| 現在の公開方法 | ConoHaサーバーへ手動アップロード |
| 移行先（作業中） | **GitHub Pages**（WordPressとConoHaは廃止予定） |
| データ入力 | **スプレッドシート＋GASのスマホアプリ**（これは残す） |

**重要**：このリポジトリは「WordPressの写し」から「**このリポジトリが唯一の正本**」へ移行中です。
今後の記事追加・修正は、WordPress側ではなく **このリポジトリのHTMLを直接編集** します。

---

## 2. 絶対にやってはいけないこと

| やってはいけないこと | 理由 |
|---|---|
| **APIキー・認証情報をチャットに表示する／スクショを要求する** | 過去に漏洩インシデントあり。**最も厳格なルール** |
| **スコアや事実を創作する** | スコアはKenjiさん本人の主観評価。**AIが数字を作るのは禁止**。不明なら「不明」 |
| **記事で特定の企業・人物を名指しで批判する** | **名誉毀損リスク**。→ `docs/記事執筆ルール.md` のSUSURUルール厳守 |
| **本番のWordPressを変更する** | ログイン・投稿・削除・設定変更は一切しない。読み取りのみ |
| **`sauna-daigaku.html` を一括置換の対象に含める** | このファイルだけ独自構造（後述）。他ページ用のスクリプトを流すと壊れる |
| **`reviews/index.html` と `サイトマップ/index.html` を手で編集する** | 自動生成ファイル。次回スクリプト実行で上書きされる |
| **WordPress由来のHTMLの「タグ閉じ忘れ」を直そうとする** | 44ファイルに元からある。実害なし。触ると差分が爆発する（後述） |
| **原因を特定せずに推測で直し、Kenjiさんに検証を頼む** | 過去に25回超の無駄な再デプロイで強い不信を招いた |
| **記事のトーン・スコアリング・SUSURUルールを勝手に変更する** | サイトの根幹的な合意事項 |
| **公開済み記事に手戻りを発生させる** | Kenjiさんの手作業が増える |
| **v12以外のスプレッドシートに書き込む** | v10・v11は退役済み |
| **Cocoon設定で「スキン」を変更する** | 「なし」で固定 |
| **`docs/STATE.md` を更新せずに作業を終える** | 次の担当者（人間もAIも）が何が起きたか分からなくなる |
| **ビルドツール・フレームワーク・npmを導入する** | 利用者はプログラミング初心者。素のHTMLのまま保つことが最優先 |

---

## 3. 基本ルール

1. **作業を始めるときは、必ず `docs/STATE.md` を先に読むこと**
2. **利用者はプログラミング初心者。専門用語を避けて日本語でやさしく説明すること**
   - 「デプロイ」→「公開」、「マージ」→「反映」のように言い換える
   - コマンドを提示するときは、何をするコマンドか1行そえる
3. **作業のたびに `docs/STATE.md` を最新にすること**
   - あわせて `README.md` と関連する手順書も、内容がズレていないか確認する
4. **何かを決めたら、理由を1行そえて `docs/STATE.md` の「決定事項と理由」に書くこと**
5. **迷ったら勝手に決めず、利用者に質問すること**
6. **一度に大きく変えず、小さく進めること**
7. **変更したら必ず動作確認すること** → `docs/作業手順とチェック方法.md` の「毎回やる3つのチェック」

---

## 3-2. ★Kenjiさん（利用者）の作業スタイル★

**これは「好み」ではなく「絶対ルール」です。過去に何度も摩擦が起きた事項が含まれます。**

### コミュニケーション

| ルール | 内容 |
|---|---|
| 言語 | **日本語必須** |
| 環境 | Windows / Chrome ＋ スマホ。**コーディング初心者** |
| 🔴 聞き直さない | **過去のやり取りで分かることを聞き直すと、明確に怒られます** |
| 🔴 自発的に進める | 不要な質問はせず、AI側が判断して進める。確認の往復を嫌う |
| バッチ処理 | まとめて大きい単位で処理することを好む |
| 作業順序 | **AI側が決める。** 「何からやりますか？」と聞かない |
| 一度に1タスク | 並行して複数を進めない |

過去に言われた言葉：「てかもういいわ」「もっと効率よくやれないの？」
——これが出たら、聞きすぎ・遅すぎのサインです。

### 成果物の形式

- **完成品を強く好む。** 「Ctrl+A → Delete → 貼り付け」で済む形で渡す
- **部分的な編集指示（「○行目を△に変えて」）は避ける。** 全体を渡す
- コードが長い場合は**チャットに直接出さずファイルとして納品**（チャットは長いコードを切る）
- **HTML全文貼り付け方式**が、UIをクリックで誘導する方式よりずっとうまくいく

### やってはいけないこと

- 🔴 **APIキー・トークン・認証情報をチャットに表示する**
- 🔴 **GASのスクリプトプロパティ画面のスクリーンショットを要求する**（平文でキーが見える）
- 🔴 **原因を特定せずに「たぶんこれ」で修正し、検証を頼む**
  （過去に25回以上の無駄な再デプロイでKenjiさんが強く消耗した経緯あり
  → `docs/過去の失敗と教訓.md`）

### 良いところ

深夜に作業することがあります。効率が悪いとストレートに指摘しますが、
**成果が出ると素直に喜び、感謝も伝えてくれる人**です。

---

## 4. ファイル構成（全体像）

```
gomirachelin/
├── AGENTS.md                    ← このファイル。AIエージェントへの指示書
├── README.md                    ← リポジトリの概要
├── CNAME                        ← GitHub Pages用。独自ドメイン名を1行だけ書いたファイル
├── .nojekyll                    ← GitHub Pages用。Jekyll処理を止める（中身は空）
│
├── index.html                   ← トップページ
├── 404.html                     ← ページが見つからないときの案内（GitHub Pages用）
├── sauna-daigaku.html           ← ★特殊★ サウナ大学の単独ページ（後述）
├── sitemap.xml                  ← 検索エンジン向けURL一覧（XMLサイトマップ）
├── sauna-icon-180.png           ← スマホのホーム画面アイコン（180x180）
├── sauna-app.webmanifest        ← ホーム画面追加時の設定
│
├── css/site.css                 ← 共通デザイン（黒×金）。ほぼ全ページがこれを読む
├── data/urls.json               ← 全ページのメタ情報（id/slug/title/link/date/type）
│
├── docs/                        ← ドキュメント類（AIも人も読む）
│   ├── STATE.md                    ← ★毎回読む★ 現状・次にやること・決定事項
│   ├── プロジェクトの理念と背景.md    ← ★技術より先に読む★ サイトが存在する理由
│   ├── 記事執筆ルール.md             ← 文体・SUSURUルール（法的安全）・スコアリング
│   ├── サイト構成.md                ← サイトの構造とデータの持ち方の詳細
│   ├── 作業手順とチェック方法.md      ← 動作確認の手順とハマりどころ
│   ├── 過去の失敗と教訓.md           ← 同じ失敗を繰り返さないための記録
│   ├── データ元システム.md           ← スプレッドシート・GAS・WordPressの詳細
│   ├── GASとGitHubの役割分担.md      ← 何をGASに任せ、何をGitHubでやるか
│   ├── GitHub-Pages移行手順.md      ← WordPress廃止・GitHub Pages移行の作業手順
│   ├── 運営とマネタイズ.md           ← アフィリエイト・未完了タスク
│   └── 新規レビュー記事の追加手順.md   ← サウナを1軒追加する手順
│
├── scripts/                     ← ページ自動生成スクリプト（Python3、標準ライブラリのみ）
│   ├── gen_reviews.py           ← reviews/index.html を作り直す
│   └── gen_sitemapp.py          ← サイトマップ/index.html を作り直す
│
├── gas/                         ← GASにコピーして使うコード（サイト表示には無関係）
│   ├── README.md                ← 導入手順
│   └── github_post.gs           ← 記事をGitHubへ投稿するモジュール
│
├── ランキング/index.html          ← ★最重要★ 施設データ FACILITIES がここにある
├── 10項目別ランキング/index.html   ← 10項目それぞれの順位（全部手書き）
├── reviews/index.html           ← 【自動生成】レビュー一覧（新着順）
├── サイトマップ/index.html         ← 【自動生成】全ページ一覧（五十音順）
├── why/index.html               ← サイトの理念
├── sauna-university/index.html  ← サウナ大学（sauna-daigaku.html とは別物）
│
├── お問い合わせ/index.html
├── 運営者情報/index.html
├── プライバシーポリシー/index.html
├── 免責事項/index.html
│
└── {施設名}/index.html × 50      ← 個別レビュー記事（フォルダ名は日本語のまま）
    例: らかんの湯/, 神戸サウナ/, 大阪サウナdesse/ …
```

**数字のまとめ（2026-09-04時点）**
- HTMLファイル：63個
- 個別記事（`type: "post"`）：50本
  - うち施設レビュー：47軒
  - コラム1本（`サウナのふるさと納税｜実質2000円でととのう、コ`）
  - 同じ施設の重複記事2本（`オアシスサウナ-アスティル-2`、`上野ステーションホステル-オリエンタル2-2`）
- ランキング掲載施設：176軒（うち記事があるのは47軒）

---

## 5. データの持ち方（ここが一番大事）

このサイトには **データベースがありません**。施設データは以下の場所に散らばっています。
**どこを直せばどこに反映されるか** を理解しないと、必ず表示がズレます。

> **⚠️ 上流にもう1つの正本があります。**
> このリポジトリの外に **スプレッドシート「GOMIRACHELIN_v12」**（200行超×43列）があり、
> そちらがKenjiさんの入力する **真の正本** です。
> リポジトリ内の `FACILITIES`（176件）は、そこから書き出した**スナップショット**です。
> **スプレッドシートを更新してもサイトには自動反映されません。**
> → 詳細は `docs/データ元システム.md`、今後の方針は `docs/GASとGitHubの役割分担.md`

### 5-1. 施設データの正本（リポジトリ内） = `ランキング/index.html` の `FACILITIES`

`ランキング/index.html` の中ほどに、こういうJavaScriptの1行があります（とても長い行）。

```javascript
var FACILITIES = [{"name":"草加健康センター","pref":"埼玉","price":1250,"min":55,"yen_h":78,"total":70,"cospa":45,"taipa":23.7,"slug":"草加健康センター-6","closed":false,"official":"https://yunoizumi.com/souka/"}, … ];
```

**これが施設データの唯一の正本です。** 176件入っています。

| キー | 意味 | 例 | 備考 |
|---|---|---|---|
| `name` | 施設名（表示用） | `"神戸サウナ"` | |
| `pref` | エリア | `"神戸"` | 都道府県とは限らない（「新橋」「巣鴨」等もある） |
| `price` | 平日料金（円） | `3100` | |
| `min` | 主要都市からの所要時間（分） | `15` | |
| `yen_h` | 1時間あたり料金（円/h） | `172` | |
| `total` | 総合スコア | `66` | ランキング「総合」タブの並び順 |
| `cospa` | コスパスコア | `33.4` | ランキング「コスパ」タブの並び順 |
| `taipa` | タイパスコア | `36.5` | 一部の施設で `0` のまま（未入力の可能性あり） |
| `slug` | 記事フォルダ名 | `"神戸サウナ"` | **空文字 `""` なら記事リンクなし** |
| `closed` | 閉店したか | `false` | `true` だと「閉店」バッジが出る（現在3軒） |
| `official` | 公式サイトURL | `"https://…"` | 「公式」ボタンのリンク先 |

**`slug` はフォルダ名と完全一致でなければならない。** ズレるとリンク切れになります。
記事がまだ無い施設は `slug` を `""` にしておけば、名前だけ載ってリンクは付きません。

### 5-2. データがどこへ流れるか

```
ランキング/index.html の FACILITIES ─┬→ ランキング/index.html 自身（JSでその場に描画）
   （施設データの正本・176件）        │
                                    ├→ scripts/gen_reviews.py  → reviews/index.html
   data/urls.json ──────────────────┤     （記事フォルダが実在する47軒を新着順）
   （公開日・タイトル）                │
                                    └→ scripts/gen_sitemapp.py → サイトマップ/index.html
                                          （記事フォルダが実在する50本を五十音順）

※ 以下は自動化されていない。手で直す必要がある：
   - index.html のトップ3            （手書き）
   - 10項目別ランキング/index.html    （10個のリストが全部手書き）
   - sitemap.xml                     （手書き）
   - data/urls.json                  （手書き）
```

### 5-3. `10項目別ランキング/index.html` は全部手書き（最大の手間）

`id="axis-s1"`（①サウナ）から `id="axis-s10"`（⑩ホスピタリティ）まで、10個のリストが
**すべて手書きのHTML** で並んでいます。各リストに48行ずつ（47軒がリンクあり＋1軒がリンクなし）。

1軒追加すると **10か所すべてに行を足し、それぞれ順位番号を振り直す** 必要があります。
ここは将来自動化したい箇所です（`docs/STATE.md` の「気になっている点」に記載）。

---

## 6. HTMLの共通構造

`sauna-daigaku.html` を除く全ページが、同じヘッダー・フッターを持っています。

### 6-1. ヘッダー（メニュー）

```html
    <nav class="nav-links" aria-label="メインメニュー">
        <a href="/">トップ</a>
        <a href="/why/">WHY</a>
        <a href="/ランキング/">ランキング</a>
        <a href="/sauna-university/">サウナ大学</a>
        <a href="/お問い合わせ/">お問い合わせ</a>
        <a href="/サイトマップ/">サイトマップ</a>
    </nav>
```

今いるページのリンクには `class="is-active"` が付きます。

### 6-2. フッター

```html
    <ul class="footer-links">
          <li><a href="/why/">WHY</a></li>
          <li><a href="/ランキング/">ランキング</a></li>
          <li><a href="/10項目別ランキング/">EVALUATION</a></li>
          <li><a href="/sauna-university/">サウナ大学</a></li>
          <li><a href="/お問い合わせ/">お問い合わせ</a></li>
          <li><a href="/運営者情報/">運営者情報</a></li>
          <li><a href="/プライバシーポリシー/">プライバシーポリシー</a></li>
          <li><a href="/免責事項/">免責事項</a></li>
          <li><a href="/サイトマップ/">サイトマップ</a></li>
    </ul>
```

### 6-3. メニューを増やす・変えるとき

62ファイルが同じパターンを持っているので、**Pythonの一括置換スクリプトを書くのが確実**です。
やり方とサンプルコードは `docs/作業手順とチェック方法.md` の「メニューを一括で変える」を参照。

**必ず `sauna-daigaku.html` を除外すること。**

### 6-4. hrefの書き方が2系統ある（注意）

| 書き方 | 例 | 使っているファイル |
|---|---|---|
| 日本語そのまま | `href="/ランキング/"` | 62ファイル（大多数） |
| %エンコード | `href="/%E3%83%A9%E3%83%B3%E3%82%AD%E3%83%B3%E3%82%B0/"` | 2ファイル（自動生成ページの一部） |

**どちらもブラウザ上は同じURLとして動きます。** ただし一括置換スクリプトを書くときは、
片方だけ検索してもう片方を取りこぼす事故が起きやすいので注意してください。

---

## 7. ★特殊ファイル：`sauna-daigaku.html`

**このファイルだけ、他とまったく違う作りです。一括処理から必ず除外してください。**

| 項目 | 内容 |
|---|---|
| 行数 | 約4,900行の巨大な単独ファイル |
| CSS | `css/site.css` を **読んでいない**（自前のスタイルを内包） |
| メニュー | `<a>` ではなく `<button data-nav="home" onclick="go('home')">` 形式のJavaScript製 |
| 構造 | 1ファイル内でページを切り替えるアプリのような作り（SPA的） |
| 中身 | サウナ大学の講義コンテンツ、クイズ、用語集など |
| 保持理由 | Amazonアフィリエイトタグ `gomirachelin-22` を含む。公開中の状態をそのまま保存している |

`sauna-university/index.html` という似た名前のページが別に存在しますが、**これは別物**です
（WordPress由来の通常ページ）。両方残っているのは意図的です。

---

## 8. デザインの決まりごと

`css/site.css` の先頭に定義されています。

```css
:root{
  --bg:#0f0d0a; --bg-2:#16120c; --surface:#1c1710; --surface-2:#241d13;
  --line:#38301e; --line-soft:#2a2316;
  --gold:#d4a017; --gold-bright:#edc65a; --gold-deep:#9c7410;
  --cream:#ece4d2; --cream-dim:#b8ae97; --muted:#877d66;
  --serif:"Shippori Mincho",serif;      /* 見出し */
  --label:"Oswald",sans-serif;           /* 英字ラベル・数字 */
  --body:"Zen Kaku Gothic New","Noto Sans JP",sans-serif;  /* 本文 */
  --maxw:1080px;
  --r:14px;
}
```

- 基調は **黒背景（`#0f0d0a`）× 金（`#d4a017`）**
- 新しいページを作るときは、既存ページのヘッダー／フッターをコピーし、`css/site.css` を読み込めば
  自動的に統一されたデザインになります
- ランキング風のリスト表示をしたいときは `.ranking-static` 配下のクラス（`.rank-row` `.pos` `.info`
  `.subs` `.score` など）を再利用できます。定義は `css/site.css` の135行目以降

---

## 9. その他の重要情報

| 項目 | 値 |
|---|---|
| Google Analytics (GA4) | `G-GTF2GM6DNJ`（全ページのheadに埋め込み済み） |
| Amazonアソシエイトタグ | `gomirachelin-22`（`sauna-daigaku.html` 内） |
| Webフォント | Google Fonts（Oswald / Shippori Mincho / Zen Kaku Gothic New） |
| 実行環境 | Python 3 の標準ライブラリのみ。追加インストール不要 |

---

## 10. 作業を終える前のチェックリスト

- [ ] リンク切れチェックを実行した（→ `docs/作業手順とチェック方法.md`）
- [ ] ローカルサーバーで実際に表示を確認した
- [ ] 自動生成ページを手で編集していない（`reviews/`、`サイトマップ/`）
- [ ] `sauna-daigaku.html` を意図せず変更していない（`git diff --stat` で確認）
- [ ] `docs/STATE.md` を更新した（やったこと・次にやること・決めたことと理由）
- [ ] `README.md` や関連手順書とズレていないか確認した
- [ ] コミットして、指定のブランチにプッシュした
